extends layout
block content
    h1 #{article.title}
    h2 #{article.author}
    p #{article.body}

    br
    a(href="/articles/edit/" + article.id) Edit
    br
    a.delete-article(href="#" data-id=article.id) Delete
    